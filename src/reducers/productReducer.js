export function productReducer(state, action) {
    switch (action.type) {
        case "":
            return {
                state
            }
    
        default:
            return state;
    }
}